<Window x:Class="FBXtoRVT.UI.NumberInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="숫자 입력" Height="190" Width="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 안내 문구 (클릭한 배관 정보 등) -->
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                   x:Name="HintText" TextWrapping="Wrap" Margin="0,0,0,12"
                   Foreground="#555555"/>

        <!-- 숫자 입력 -->
        <TextBlock Grid.Row="1" Grid.Column="0" x:Name="LabelText"
                   VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox   Grid.Row="1" Grid.Column="1" x:Name="ValueBox" Margin="0,0,0,10" Padding="4,3"
                   PreviewTextInput="ValueBox_PreviewTextInput"/>

        <!-- 버튼 -->
        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="OK" Width="80" Height="28" Margin="0,0,8,0" IsDefault="True" Click="Ok_Click"/>
            <Button Content="Cancel" Width="80" Height="28" IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
